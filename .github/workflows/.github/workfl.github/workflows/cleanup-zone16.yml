name: Cleanup Zone 16

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  cleanup-zone16:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: v1.0

      - name: Remove obsolete Zone 16 documents
        shell: bash
        run: |
          DIR="docs/SYSTEM ZONES/16-documentation-and-registration-system"

          git rm --ignore-unmatch "$DIR/imp-16-05-01-standard-statusa-dokumentacije.md"
          git rm --ignore-unmatch "$DIR/imp-16-05-02-postupak-pregleda-i-odobravanja.md"
          git rm --ignore-unmatch "$DIR/imp-16-05-03-registar-odobrenih-dokumenata.md"

          git rm --ignore-unmatch "$DIR/imp-16-06-01-okvir-arhivskog-sustava.md"
          git rm --ignore-unmatch "$DIR/imp-16-06-02-standard-dugorocnog-arhiviranja.md"
          git rm --ignore-unmatch "$DIR/imp-16-06-03-postupak-povrata-arhivirane-dokumentacije.md"

          git rm --ignore-unmatch "$DIR/imp-16-07-01-standard-meudokumentacijskih-veza.md"
          git rm --ignore-unmatch "$DIR/imp-16-07-02-matrica-povezanosti-dokumenata.md"
          git rm --ignore-unmatch "$DIR/imp-16-07-03-postupak-provjere-referenci.md"

          git rm --ignore-unmatch "$DIR/imp-16-08-01-arhitektura-digitalnog-repozitorija.md"
          git rm --ignore-unmatch "$DIR/imp-16-08-02-standard-pohrane-digitalne-dokumentacije.md"
          git rm --ignore-unmatch "$DIR/imp-16-08-03-plan-sigurnosnih-kopija-i-oporavka.md"

          git rm --ignore-unmatch "$DIR/imp-16-09-01-standard-dokumentacijske-kontrole.md"
          git rm --ignore-unmatch "$DIR/imp-16-09-02-postupak-provjere-cjelovitosti-dokumentacije.md"
          git rm --ignore-unmatch "$DIR/imp-16-09-03-izvjestaj-o-dokumentacijskim-odstupanjima.md"

          git rm --ignore-unmatch "$DIR/imp-16-10-01-okvir-dokumentacijske-analitike.md"
          git rm --ignore-unmatch "$DIR/imp-16-10-02-kpi-dokumentacijskog-sustava.md"
          git rm --ignore-unmatch "$DIR/imp-16-10-03-program-razvoja-dokumentacijskog-sustava.md"

      - name: Commit cleanup
        shell: bash
        run: |
          if git diff --cached --quiet; then
            echo "No obsolete Zone 16 files found."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git commit -m "Finalize Zone 16 canonical documentation"
          git push origin HEAD:v1.0
